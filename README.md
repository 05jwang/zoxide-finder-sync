# zoxide-finder-sync

## Script usage

1. In a terminal:

```bash
chmod a+x scripts/zoxide_finder_tracker.sh
source scripts/zoxide_finder_tracker.sh
```

2. Click `Allow`

3. Navigate around finder. Check that the directories are printed out in the terminal.
